import { DefaultTheme } from "@react-navigation/native";

export const Theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#6C63FF",
    onPrimary: "rgb(255, 255, 255)"
  }
}